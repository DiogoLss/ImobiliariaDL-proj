export default interface FiltrosParameters
{
    cidade: number | null,
    bairro: number | null,
    tipo: number | null
}
export interface FiltrosParametersDropdown
{
    min: number ,
    max: number 
}